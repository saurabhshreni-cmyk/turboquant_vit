# Streamlit Cloud installs from this file. We pin the CPU torch wheels via
# the extra index so the deploy fits in the free-tier resource budget — the
# default torch wheel pulls ~800 MB of CUDA libraries we don't need.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.32.0
torch>=2.2.0
torchvision>=0.17.0
transformers>=4.40.0
numpy>=1.26.0
pandas>=2.2.0
plotly>=5.20.0
Pillow>=10.3.0
scipy>=1.12.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
